# Core pipeline
pypdf>=4.0.0            # PDF parsing
python-docx>=1.1.0      # DOCX parsing (bonus format)
pydantic>=2.6.0         # Structured schemas (incl. LLM structured output)
httpx>=0.27.0           # GitHub API + LLM HTTP calls (timeouts, retries)
python-dotenv>=1.0.0    # Load .env without hard-coding secrets

# Optional API interface (bonus)
fastapi>=0.110.0
uvicorn>=0.29.0

# Dev / test / sample-data generation
pytest>=8.0.0
fpdf2>=2.7.0            # Generates the synthetic sample resume set
